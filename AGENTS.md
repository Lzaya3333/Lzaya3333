# Codex 项目规则：CAD 办公家具自动出图

本仓库用于让 Codex 处理办公家具 CAD 自动建模、DXF 出图、BOM 料单、孔位和装配检查。

## 总原则

1. 所有图纸、三维模型、BOM、孔位、检查报告必须来自同一套数据模型。
2. 每块板件必须是独立对象，不允许把所有板件合并成一个整体。
3. 不允许凭图片随意猜板件；图片只能作为外观参考，料单和结构规则是标准答案。
4. 任何实体板件必须进入 BOM；孔位、封边、辅助线、调试文字不能进入 BOM。
5. 修改模型前先输出检查报告；修改后必须重新跑检查。
6. 正式模型不显示板件表面文字；调试文字只能放在 debug 文件或 debug 图层。
7. 单位统一为 mm，尺寸误差默认不得超过 0.1mm。

## 推荐技能调用

- 做家具三维结构：使用 `$cad-furniture-modeling`
- 做板件拆分和 BOM：使用 `$cad-board-bom`
- 检查板件打架、错位、悬空：使用 `$cad-assembly-check`
- 做孔位：使用 `$cad-hole-system`
- 根据截图或报告修错误：使用 `$cad-error-fix`

## 输出习惯

每次生成或修改模型时，至少输出：

- `actual_parts.json`
- `bom.csv`
- `assembly_fit_report.txt` 或对应检查报告
- 正式模型 `*_clean.dxf`
- 调试模型 `*_debug.dxf`

## 禁止事项

- 不要把封边生成成实体板件。
- 不要把孔位生成成 Board 板件。
- 不要用 LINE/POLYLINE 代替正式三维板件。
- 不要跳过检查报告直接说模型已完成。
- 不要在 clean 正式模型里保留 TEXT/MTEXT 调试文字。